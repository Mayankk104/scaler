{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "debugging",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "preLaunchTask": "build active file"
        }
    ]
}
